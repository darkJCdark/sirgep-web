# sirgep-app
SIRGEP is a web system with layered architecture: backend in Java (NetBeans 21, JDK 21, Maven, SOAP) and frontend in ASP.NET with C#. It manages users, municipal events and spaces, tickets, reservations, payments, notifications, dashboards, and PDF receipts. It uses MySQL on AWS, deployed on Ubuntu/Windows, with credentials encrypted.
